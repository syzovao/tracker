{% extends '::base.html.twig' %}

{% block body -%}
    <h1>IssueComment edit</h1>
    {{ form(edit_form) }}
    <ul class="record_actions">
        <li>
            <a href="{{ path('oro_issue_view', { 'id': entity.issue.id }) }}">
                Back to issue
            </a>
        </li>
        {{ form(delete_form) }}
    </ul>
{% endblock %}
